# inicdents
This repo will be used to store the code used for learning purpose.


git config --global user.name "kubit-3009"


git config --globael user.email "kubitailabs@gmail.com"

git config --global --list
git config --list
